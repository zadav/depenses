{% extends "DavidDepenseBundle::layout.html.twig" %}


{% block body %}
<h2>Ajout d'une d&eacute;pense</h2>
	{{ form_start(form) }}
                {{ form_errors(form) }}
                <div>
                    {{ form_label(form.jour) }}
                    {{ form_errors(form.jour) }}
                    {{ form_widget(form.jour) }}
                </div>
               <div>
                    {{ form_label(form.bilan) }}
                    {{ form_errors(form.bilan) }}
                    {{ form_widget(form.bilan) }}
                </div>
        {{ form_end(form) }}
{% endblock %}